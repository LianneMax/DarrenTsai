node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
SESSION_HANDOFF_PRIVATE.md

# Local Netlify folder
.netlify

# Written by the Netlify CLI when it runs functions locally, not by the build.
deno.lock

# Vitest JSON reporter output
.vitest/

# Agent skills installed by `npx skills add`. Machine-local: .claude/skills
# holds absolute-path symlinks into .agents/, so committing these would break
# on any other checkout. Reinstall with the commands in skills-lock.json.
.agents/
.claude/
skills-lock.json
